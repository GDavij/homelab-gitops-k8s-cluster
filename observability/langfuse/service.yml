---
apiVersion: v1
kind: Service
metadata:
  name: langfuse
  namespace: observability
  annotations:
    argocd.argoproj.io/sync-wave: "4"
spec:
  selector:
    app: langfuse
  ports:
    - port: 3000
      targetPort: 3000
      name: http
