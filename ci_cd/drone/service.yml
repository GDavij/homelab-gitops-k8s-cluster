---
apiVersion: v1
kind: Service
metadata:
  name: drone
  namespace: ci-cd
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  selector:
    app: drone
  ports:
    - port: 80
      targetPort: 80
      name: http
